
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Regla simplificada para asegurar acceso a la carpeta de reportes
    match /reports/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
